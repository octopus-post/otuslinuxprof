server {
    listen 443 ssl;
    server_name monitor.loc;

    ssl_certificate /etc/ssl/{{ ansible_hostname }}/monitor.loc.crt;
    ssl_certificate_key /etc/ssl/{{ ansible_hostname }}/monitor.loc.key;

    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
